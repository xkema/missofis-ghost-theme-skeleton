// 
Zepto( function( $ ) {

	/*

	// 
	$( document ).on( 'swipe', function( e ) {

		alert( 'swiped' );

	} );

	// navigation toggle button controls
	$( '.nav-toggle' ).on( 'click', function( e ) {

		e.stopImmediatePropagation();
		$this = $( this );
		$navSite = $( '.nav-site' );
		$navSite.toggle();

	} );

	// hide navigation on document click
	$( document ).on( 'click', function( e ) {

		if( 'none' !== $( '.nav-toggle' ).css( 'display' ).trim() ) {
			$navSite = $( '.nav-site' );
			$navSite.hide();
		}

	} );

	// window resized (bugfix for orientation change navigation ...)
	$( window ).resize( function( e ) {

		if( 'none' === $( '.nav-toggle' ).css( 'display' ).trim() ) {
			$navSite = $( '.nav-site' );
			$navSite.show();
		}


	} );

	*/


} );